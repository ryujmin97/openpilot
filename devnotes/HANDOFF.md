# HANDOFF

Worker: Claude (39차 -- 화면녹화 탭 사진 업로드 UI 신규 구현 + 경로안내 박스 상하 여백 통일)
Date: 2026-09-15
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (이 세션 커밋으로 갱신, 반영 스크립트 실행 필요)
Note Branch: carrot-ryu-note (이 커밋으로 devnotes 갱신)
carrot-ms 마지막 검토/동기화 커밋(메시지 기준): 7차 세션과 동일, 신규 커밋 없음(이번 세션에서도 재확인하지 않음)

작업:
사용자가 38차에서 미확인으로 이월됐던 "화면녹화 탭 업로드 UI 자체 동작"의 의도를 정정 -- 녹화본이 아니라 온로드 화면캡쳐 사진 업로드를 원했던 것으로, 사진 목록에 체크박스(앞)/다운로드+전송 버튼(뒤) 및 상단 전체선택/다운로드/전송 툴바를 신규 요청. 동시에 34차에서 이월됐던 경로안내 박스 상하 여백 불균형(상단 제목 여백은 넉넉한데 하단 신호과속/도로명 배지가 박스 경계에 닿거나 벗어나 보임)도 함께 요청. 두 항목 모두 이전 세션에서 코드 작업/정적 검증을 마쳤으나 반영 스크립트 전달 전에 무료 사용량 한도로 세션이 종료되어, 이번 세션에서 스크립트 두 종(carrot-ryu 코드, carrot-ryu-note devnotes)을 마무리해 전달.

완료:
- 화면녹화 탭 "사진" 목록을 가로 썸네일 스트립에서 체크박스/다운로드/전송 버튼이 있는 세로 행(row) 목록으로 전면 재구현(screenshots.js, runtime.js, index.html, style.css, 3개 언어 번역, server/features/screenrecord/routes.py에 업로드 엔드포인트 신규 추가). 영상 목록(screenrecord.js)과 동일한 컨벤션 재사용.
- 경로안내 박스(hud_renderer.py) `content_shift_y = 20` 상수 도입으로 박스 내 모든 컨텐츠를 동일한 양만큼 위로 이동, 상하 여백을 맞춤. 요소 간 상대 간격은 그대로 유지.
- 정적 검증 전부 통과: py_compile(hud_renderer.py, routes.py), node --check(runtime.js, screenshots.js, 번역 3종), npm install && node build.mjs(생성 번들 재생성), npm test 737/737.
- **[이번 세션 추가 검증]** 이전 세션이 만든 반영 스크립트(carrot-ryu용)를 이번 세션에서 새로 clone한 GitHub 최신 상태(HEAD `c01d9ec`, 37차와 동일 -- 즉 이 스크립트가 아직 실행되지 않았음을 재확인) 위에 Python으로 스크립트의 문자열 치환(Apply-Replace) 로직을 그대로 재현해 실제로 적용, 17개 anchor 전부 정확히 1회 매치 + 치환 성공을 확인한 뒤 py_compile/node --check/build/npm test(737/737)를 처음부터 다시 전부 통과시킴(이전 세션의 사전 검증을 다른 세션이 독립적으로 재검증).
- **[이번 세션에서 발견/수정]** 이전 세션이 만든 carrot-ryu 스크립트(.ps1)에서 9절·18절 필수 규칙 위반 3건을 발견해 수정: (1) `git clone`에 `--config core.autocrlf=false`(27차 필수 규칙)가 빠져 있던 것을 추가, (2) 한글이 포함된 `.ps1` 파일 자체에 UTF-8 BOM(21차 필수 규칙)이 없던 것을 추가, (3) 스크립트 마지막에 임시 폴더를 삭제하지 않고 "수동으로 지우라"고 남겨두던 것(18절 금지 항목 위반)을 `Remove-Item -Recurse -Force $Tmp`로 수정해 스크립트가 스스로 정리하도록 함.

미완료 (다음 세션 이월):
1. [신규] 39차 사진 업로드 UI 실기기 검증 -- 체크박스 선택/전체선택/개별 및 선택 다운로드/전송이 실제 온로드 캡쳐 사진 파일로 정상 동작하는지 확인 필요.
2. [신규] 39차 경로안내 박스 여백 실기기 재확인 -- `content_shift_y = 20`으로 상하 여백이 실제로 균등해졌는지 스크린샷으로 확인, 필요 시 값 조정.
3. [이월] 화면녹화 탭 "영상" 업로드 UI(36차 구현분) 자체 동작 검증 -- 실제 화면녹화 파일 확보 후 재검증 필요(38차에서도 녹화본 부재로 미확인).
4. [이월] 37차 락 수정의 실제 동시성(거의 동시 호출) 재현 검증 -- 대시캠 탭과 화면녹화 탭(또는 사진) 전송을 의도적으로 동시에 눌러보는 재현 테스트 필요.
5. [이월] 34차 도로명-신호과속 같은 줄 배치 확인 -- 신호과속 배지가 뜨는 구간에서 재검증 필요.
6. [이월] 28~30차 레이아웃 정밀 재검증(좌표 단위 정밀 확인 아님, 육안 확인만 완료).
7. [이월] 실차 재검증(8~39차 코드 변경 전부, 12절 원칙) -- 계속 이월.
8. [이월] 실기기에서 직접 디버깅: 배포된 tools.js에 "web-gdrive-connect" 문자열 실제 존재 여부(26차부터 이월).
9. [이월] test_web_upload.py 실제 실행해 낡은 테스트 수 확인 -> 데드코드 3개 + 대응 테스트 삭제/갱신(25차부터 이월).
10. [이월] docs/carrot_web_upload.md 갱신(Drive 기준)(15차부터 이월).
11. [이월] run_upload_segments() 설계 변경 실사용 문제 없는지 재확인(16차부터 이월).
12. [이월] carrot-ms 모델 셀렉터 코드 분석 착수(6차 이후 계속 미착수).

검증: 위 "완료" 항목에 정적 검증(py_compile/node --check/build/test) 및 이번 세션의 독립 재검증 전부 명시. 실기기 검증은 미실시(12절 원칙) -- 사진 업로드 UI 동작과 경로안내 박스 여백 실측은 다음 세션 이월.

주의사항:
- raw.githubusercontent.com은 push 직후 브랜치-head URL이 한동안 이전 내용을 반환할 수 있음(33차, 38차 재현). 다음 세션은 push 직후 devnotes/코드 재확인 시 commit-pinned raw URL(`/{sha}/...`) 또는 commit diff 엔드포인트를 우선 사용할 것.
- `.screenrecord-photo`(단수) CSS 클래스는 39차에서 완전히 삭제됨.
- `content_shift_y`는 코드 상수이며 Params로 노출되지 않음 -- 실기기 여백 조정은 코드 수정 필요.
- **carrot-ryu 스크립트가 아직 실행되지 않은 상태로 이 devnotes가 먼저 기록됨(5절 "작업 흐름"에 따라 코드/devnotes 스크립트를 한 묶음으로 함께 전달하는 방식).** 다음 세션 시작 시 4절 3번 단계(carrot-ryu 최신 commit 확인)에서 이번 세션 커밋(39차)이 실제로 반영됐는지 반드시 확인할 것 -- 사용자가 스크립트 실행을 건너뛰었거나 실패했을 가능성을 배제하지 말 것(16절 원칙).

다음 작업 후보:
1. 39차 사진 업로드 UI + 경로안내 박스 여백 실기기 검증
2. 화면녹화 탭 "영상" 업로드 UI(36차) 실기기 검증(녹화본 확보 후)
3. 37차 락 수정 동시성 재현 검증(의도적으로 동시에 두 업로드 시도)
4. 34차 도로명-신호과속 같은 줄 배치 확인(신호과속 구간에서)
5. test_web_upload.py 실행 + 데드코드 정리
6. carrot-ms 모델 셀렉터 코드 분석 착수