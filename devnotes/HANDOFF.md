Worker: Claude (150cha, Claude Sonnet 5)
Date: 2026-09-24
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (base: c0a01658f109f5fb52b98f56c4ff5e6dcde42159, 147차 코드 push 완료 상태 유지 -- 이번 세션 코드 변경 없음)
Note Branch: carrot-ryu-note (이 스크립트 반영 전 base: 149차 devnotes push 완료 상태, `2ee44653dda3dc1b07148549c5be0888e24ada9e`)
carrot-ms 마지막 검토/동기화 체크포인트: `3756e6d5`(130차, 139차 세션 재확인 -- 신규 커밋 없음. 140~150차는 재점검 없음)

작업:
1. 세션 시작 4절 0단계로 지침 문서(v2, commit `2ee4465`) 조회, 149차 devnotes push(`2ee4465`, 부모 `ce81555`) GitHub 직접 재조회로 확인.
2. 사용자가 149차에서 확보한 fade 밴드 스윕 재생 자료(WIP.md 149차 표)를 검토한 뒤 현재 밴드 폭(PREVIEW_GATE_M_LO/HI=1.05/1.25)을 유지하기로 확정.
3. HANDOFF.md 미완료 2번("fade 밴드 폭 확정 -- 사용자 결정 필요") 해소.

완료:
1. fade 밴드 폭(1.05/1.25) 확정 -- 사용자 결정 완료(현행 유지). 코드는 이미 147차(`c0a01658`)에 이 값으로 반영되어 있어 코드 변경 없음. WIP.md 150차 참고.

미완료(다음 세션 최우선 순으로):
1. **147차 코드가 탑재된 디바이스의 실주행 로그 검증(최우선, 148/149차 이월)** -- 149차 재생은 로그 기록 시점에 147차 코드가 없었으므로 시뮬레이션일 뿐이다. 같은 도구(`replay_gate147.py`, `ego_extract2.py`+`extract_radar_flag.py`)로 실제 leadPreviewSeconds/게이트 거동을 비교할 것. 실차 검증 미실시.
2. "선행차가 설정 차간거리(m~1.25) 근처에서 급제동" 시나리오는 로그에 없어 정량 미검증(148/149차 이월, 위험 완전개방 구간은 밴드와 무관, 차이는 onset 지연 vs 정속 추종 억제량) -- 해당 로그 확보 시 검증.
3. 148차 v1 `2>&1` 재발(핵심 발견 53/55와 동일 패턴 3회째)의 FINDINGS.md 정식 등록 여부 결정(148차 이월).
4. 114차 계열 이월 항목(변동 없음): xTurn=6(톨게이트) 케이스 로그 미확보, 디바이스 MapTurnSpeedFactor(base) 실측값 미확인.
5. 110차 GATE_M 관련 추가 실차 사례(변동 없음).
6. 이전 이월: diff/블록 추출 스크립트(임시, 147차 코드 반영용) toolkit 미등록.

검증:
- 정적/재생: 해당 없음(코드 변경 없음, 149차 재생 결과가 이 결정의 근거).
- 실차: 미실시.

주의사항:
- fade 밴드 폭 확정은 사용자 결정이며, 근거는 149차 재생 검증(로그 기록 커밋 `8e8b0d1a` 위의 시뮬레이션)이다. 147차 코드가 실차에서 이렇게 동작한다는 증거는 여전히 아니다(12절).
- 이 devnotes 반영 스크립트(150차)의 실행/push 확인이 필요하다.

다음 작업:
1. 이 devnotes 반영 스크립트 실행/push 확인.
2. 147차 탑재 디바이스 실주행 로그가 생기면 미완료 1번 수행.
3. 148차 재발 FINDINGS 등록 여부 사용자 결정.
