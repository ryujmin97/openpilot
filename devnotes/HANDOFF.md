Worker: Claude (157cha, Claude Sonnet 5)
Date: 2026-09-24
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (HEAD: 619998bb144c699f379a5c8f6c9a64843d108f28, 156차 `cleanup_stale_upload_tmp_dirs()` push 완료 확인 -- 이번 157차는 코드 변경 없음)
Note Branch: carrot-ryu-note (이 스크립트 반영 전 base: 34dcd2ed5f8f5ccd06834c7434f019b1d80bf776, 156차 devnotes push 완료 상태)
carrot-ms 마지막 검토/동기화 체크포인트: `3756e6d5`(130차, 139차 세션 재확인 -- 신규 커밋 없음. 140~157차는 재점검 없음)

작업:
1. 세션 시작 `git ls-remote`로 carrot-ryu HEAD가 이미 `619998bb`(156차 코드 반영 커밋)임을 발견 -- HANDOFF.md(156차)에는 "push 대기"로 남아있던 괴리를 16절에 따라 재확인/정정.
2. 사용자가 제공한 실주행 rlog 38세그먼트(route `00000449--b34152b780`, 약 37.8분, 기록 커밋 `f23d05f` = 153차 코드 반영)로 미결 실차 검증 항목 3건을 처리: (a) 153차 `get_path_after_distance()` 수정 실주행 검증(154차 이월), (b) 분기/톨게이트 통과 직후(xDist<0) 152차 게이트 확인(154차 이월), (c) 147차 감속 프리뷰 게이트 실제 탑재 디바이스 로그 검증(148/149차 이월).

완료:
1. 156차 코드 push 확인: commit `619998bb`의 patch를 GitHub에서 직접 조회, 변경 파일이 `upload_jobs.py` 1개(+33/-0)뿐이고 pre/post-image blob hash(`fb534605ed`->`e5914f2841`)가 156차 HANDOFF.md의 dry-run 가드 값과 정확히 일치함을 확인(16절).
2. 153차 수정 실주행 검증: `devnotes/toolkit/route_decel/replay_route_geom.py`로 OLD(`44bfd33d`, 153차 이전)/NEW(`f23d05f`, 153차 반영)를 이 로그의 실제 GPS/navRoute/차속으로 재생 비교. 트리거 세그먼트(첫 폴리라인 세그먼트>=300m) 8개에서 route 값 급변 건수가 NEW에서 전부 대폭 감소(예: seg#23 384->3, seg#22 103->9, seg#16 77->5, seg#18 29->6, seg#10 25->4, seg#1 21->12), 비트리거 사이클은 OLD/NEW 무회귀(0건 차이). 이번 로그가 처음으로 153차 코드 자체가 탑재된 상태에서 기록된 실주행이므로 검증 완료로 판단.
3. 152차 게이트 확인(xDist<0): 분기 통과 직후(xTurn 3/4 -> -1) route 상한이 한 번 크게 뛰는 지점을 발견했으나, 이는 곡선 제약(near/far 거리 창) 해제로 인한 정상적 캡 완화이고 그 시점 desiredSpeed 실제 소스는 route가 아니라 road/cam으로 이미 넘어가 있어 최종 속도에 영향 없음을 로그 대조로 확인(문제 없음 확정).
4. 147차 감속 프리뷰 게이트 검증: `longitudinalPlan.leadPreviewSeconds` 활성 표본(6.2%) 중 활성도 상위(lps=1.5 최댓값) 표본 전부를 radarState.leadOne/carState와 대조, 전부 실제 근접·감속 리드 상황에서만 열렸고 평상 순항 중 오탐 없음을 확인. 147차 코드가 실제 탑재된 디바이스 로그로는 최초 확인.

미완료(다음 세션 최우선 순으로):
1. 156차 A안의 실기기 검증 -- 강제 종료/전원 차단을 실제로 재현한 뒤 다음 업로드 시작 시 `cleanup_stale_upload_tmp_dirs()`가 잔존 zip을 정리하는지 확인(155~156차부터 이월, 로그 분석으로는 검증 불가).
2. 톨게이트(xTurn=6) 구간 실차 검증 -- 이번 로그(38세그먼트, 55개 xTurn 전이) 전체에 xTurn=6 전이가 0건이라 여전히 미확인(114차부터 이월). 톨게이트 통과 구간이 포함된 로그 필요.
3. 잔여 비트리거 급변(세그먼트당 10~25건, 154차 발견)의 근본 원인 분석 미착수(154차 이월, 이번 세션도 OLD/NEW 무회귀만 재확인).
4. (선택) zip 내용 무결성 확인(Drive에서 내려받아 열기), 두 번째 zip(497.5MB)이 첫 번째(459.4MB)보다 큰 이유 확인(155차 이월).
5. (선택) `build_zip()` 직전 `shutil.disk_usage`로 필요 용량 vs 여유를 비교하는 조기 실패 방어 코드(미포함 상태 유지).
6. 148차 v1 `2>&1` 재발의 FINDINGS.md 정식 등록 여부 결정, 147차 임시 diff/블록 추출 스크립트 toolkit 미등록(모두 이전 이월, 변동 없음).

검증:
- 위 2~4번은 실제 코드(재구현 없이 ast 추출/exec 또는 import)로 실주행 rlog 재생·집계한 open-loop 로그 분석이며, 실기기 테스트/실주행 자체는 아니다. 156차(강제 종료 재현)와 톨게이트 케이스는 이번 세션에서 검증하지 못했다.

주의사항:
- 이번 157차는 코드 변경이 없다(carrot-ryu HEAD는 156차 그대로 `619998bb`).
- 153차/147차 검증은 "코드가 실제로 탑재된 상태에서 기록된 로그"라는 점이 이전 154/148/149차의 open-loop 분석과 다르다 -- 다만 여전히 로그 사후 분석이지, 사용자가 실차 주행 중 이상 유무를 직접 확인·보고한 것은 아니다.
- 152차 게이트 확인은 "왕복 진동 없음 + 최종 속도 미영향"을 근거로 문제 없음을 확정했으나, 이는 이번 8개 트리거 세그먼트 표본 범위 안에서의 결론이다.

다음 작업:
1. 사용자가 아래 devnotes 반영 스크립트(`157cha_devnotes_carrot_ryu_note.ps1`)를 실행 -> push 확인.
2. 실기기에서 강제 종료/전원 차단을 재현해 156차 A안 검증.
3. 톨게이트 통과 구간이 포함된 실주행 로그 확보 시 xTurn=6 검증.