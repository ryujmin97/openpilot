Worker: Claude (155cha 계속2, Claude Sonnet 5)
Date: 2026-09-24
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (HEAD: 9ff1242e4ba1da3f491ef0a7de563647919aff63, 155차 코드 -- 기기에 pull된 것까지 확인됨. 이번 계속2 회차 코드 변경 없음)
Note Branch: carrot-ryu-note (이 스크립트 반영 전 base: 155차 계속 devnotes push 완료 상태, `71d7e1050fdb28edf1bba3f173e96b9a65773e34`)
carrot-ms 마지막 검토/동기화 체크포인트: `3756e6d5`(130차, 139차 세션 재확인 -- 신규 커밋 없음. 140~155차는 재점검 없음)

작업:
1. 155차 계속에서 "미확인"으로 남긴 3건(기기 pull SHA, Drive 업로드 완료, 임시 폴더 정리)을 사용자가 보낸 스크린샷 3장으로 확인하고 WIP.md 155차 계속2/CURRENT_STATUS.md/이 HANDOFF에 기록. 이 세션은 스크린샷을 읽었을 뿐 기기/Drive에 직접 접근하지 않음.

완료:
1. 155차(로그탭 선택 전송 ENOSPC/Errno 28) 실기기 검증 완료: 기기 HEAD `9ff1242e4` 일치(152·153·155차 코드 탑재 확정), 전체선택 선택 전송 2회 -> Drive에 459.4MB/497.5MB zip 업로드 완료, 스테이징 폴더 비어 있음(`/tmp` 1% 사용). 정차 상태 기능 확인이며 주행 검증 아님.

미완료(다음 세션 최우선 순으로):
1. **주행 실차 검증 여전히 미실시** -- 기기에 153차 수정(`get_path_after_distance()`)과 152차 게이트가 탑재된 것이 확정됐으므로, 다음 실주행 로그(분기 xTurn=4 / 톨게이트 xTurn=6 구간)로 검증 가능(154차 이월). 사용자가 로그를 주면 154차 재생 도구(`replay_route_geom.py`)로 이어서 분석.
2. 강제 종료/전원 차단 시 `/data/carrot/tmp/dashcam_upload/carrot_dashcam_*` 잔존 zip(수백 MB)이 재부팅으로 안 지워지는 문제 -- 정상 종료 경로만 검증됨. 시작 시 정리 로직 추가 여부 결정 대기: 추천안 A(작업 시작 시, 실행 중 작업이 없을 때 무활동 만료 시간보다 오래된 `carrot_dashcam_*` 삭제, 코드 약 10줄), B(서버 기동 시 전체 청소, 시작 지점 미조사), C(수동 삭제). 사용자 승인 시 9절 절차로 코드 작성.
3. (선택) zip 내용 무결성 확인(Drive에서 내려받아 열기), 두 번째 zip(497.5MB)이 첫 번째(459.4MB)보다 큰 이유 확인.
4. (선택) `build_zip()` 직전 `shutil.disk_usage`로 필요 용량 vs 여유를 비교하는 조기 실패 방어 코드(미포함 상태 유지).
5. 분기/톨게이트 통과 직후(xDist<0) 구간 요동에 152차 게이트(TurnSpeedControlMode==2)가 적용되는지 확인(154차 이월).
6. 잔여 비트리거 급변(세그먼트당 10~25건)의 원인 분석 미착수(154차 이월).
7. 147차 코드 탑재 디바이스 실주행 로그 검증, "선행차가 설정 차간거리 근처에서 급제동" 시나리오 정량 미검증, 148차 v1 `2>&1` 재발의 FINDINGS.md 정식 등록 여부 결정, 147차 임시 diff/블록 추출 스크립트 toolkit 미등록(모두 이전 이월, 변동 없음).

검증:
- 155차 코드: 정적 분석 + py_compile + 로컬 bare 저장소 시뮬레이션 + 실기기(사용자 스크린샷 기준 코드 탑재/전체선택 전송/Drive 완료/임시 폴더 정리). 주행 실차 검증은 미실시.
- 원인 수치(`/tmp` 150M, `/data` 71G, route 합계 약 460M)는 사용자가 전달한 이전 세션 분석 인용이며, Drive의 459.4MB zip이 route 합계 약 460M와 일치해 간접 재확인됨.

주의사항:
- 이번 계속2 회차는 devnotes만 변경, carrot-ryu 코드 변경 없음(코드 HEAD는 155차 `9ff1242e` 그대로).
- `/data`는 재부팅해도 비워지지 않는다(FINDINGS 핵심 발견 56 일반화 2) -- 잔존 zip 정리 로직은 아직 없다.

다음 작업:
1. 사용자가 주는 실주행 로그로 153차 수정 실차 검증(분기/톨게이트).
2. A안(잔존 zip 청소) 진행 여부 사용자 결정 후 코드 작성.