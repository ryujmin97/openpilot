# HANDOFF

Worker: Claude (48차 -- 47차 스크린샷 DPI/JPG 수정 push 확인 + 36차/39차/37차 이월 항목 실기기 검증, 코드 변경 없음, devnotes만 갱신)
Date: 2026-09-16
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (HEAD: `41fd34a7458d663d27e16d96fbeb2c6bcb20c1b5`, 47차 스크린샷 DPI/JPG 수정 -- 이번 세션에서 git ls-remote + GitHub commit API + raw 조회(SHA 고정)로 직접 재확인함. 이번 세션은 코드 변경 없음)
Note Branch: carrot-ryu-note (이 커밋으로 devnotes 갱신. base: `e58c48ad4b18cd420705209df678d843ea32fc70`, 47차)
carrot-ms 마지막 검토/동기화 커밋(메시지 기준): 7차 체크포인트 이후 신규 23건 확인, WIP_SYNC.md 40차 체크포인트 반영 스크립트 실행 여부 미확인(계속 이월)

작업:
사용자가 "체크포인트"를 요청하며 실기기 스크린샷 7장(로그 전송 확인창 2장, HUD 디바이스 직접촬영 1장, 로그탭 화면녹화/사진 목록 2장, 구글드라이브 폴더·파일 목록 2장)을 제공. 지침 문서 4절 0단계(git ls-remote SHA 고정 조회)부터 시작해 HANDOFF.md/CURRENT_STATUS.md를 먼저 확인한 뒤, 제공받은 스크린샷을 전 세션들의 이월 항목과 대조.

완료:
1. carrot-ryu HEAD가 `41fd34a7`(47차 스크린샷 수정)로 이미 push돼 있음을 `git ls-remote` + GitHub commit API로 확인 -- 47차 미완료 1번("[최우선, 신규] carrot-ryu 스크립트 실행 결과 확인")이 세션 사이에 사용자 실행으로 이미 해소돼 있었음. raw.githubusercontent.com(SHA 고정)으로 `screenshot_capture.py` 실제 내용까지 재조회해 `rl.load_image_from_screen()` 교체 + `.jpg` 저장이 계획대로 반영됐음을 확인.
2. 제공받은 스크린샷으로 이전 세션 이월 항목 실기기 검증:
   - Issue 1(당근서버 라벨 오표시, 36차 수정): 로그 전송 확인창에 "구글 드라이브"로 정확히 표시됨을 직접 확인 -- 정상.
   - Issue 3(햄버거 메뉴가 탭 무관 대시캠 전용 업로드, 36차 수정): 화면녹화/로그 탭에서 탭 전용 선택-전송 UI(체크박스 + "선택 전송" 버튼)가 정상 동작함을 확인 -- 정상.
   - 39차/46차에서 미확인으로 남아있던 "선택 전송" 버튼의 실제 전송 성공 여부: 사진/영상 3개 항목을 체크 후 전송하는 흐름을 확인했고, 구글드라이브 "CarrotWeb Logs" 폴더에 해당 타임스탬프 mp4 파일(예: `20260916-111847.mp4`)이 실제로 존재함을 확인 -- 전송 성공까지 실증됨.
   - Issue 4(Drive 폴더 중복생성, 37차 락 수정): 구글드라이브 루트에 "CarrotWeb Logs" 폴더가 1개만 존재함을 재확인 -- 정상.
   - 27차 HUD 경로안내 박스: 디바이스 화면 직접촬영 사진에서 우측하단 박스(교차로/회전아이콘/route=/도착거리/ETA/도로명) 레이아웃이 정상임을 재확인.
3. 코드 변경 없음(전부 검증/문서화 세션). WIP.md(이어붙이기)와 FINDINGS.md(이어붙이기), CURRENT_STATUS.md/HANDOFF.md(전체교체) devnotes 반영 스크립트만 작성.

미완료 (다음 세션 이월):
1. [최우선, 이월] 47차 스크린샷 수정 자체의 실차 검증 -- 실제 스크린샷 버튼을 눌러 (a) 가로 2160x1080으로 저장되는지, (b) 검정 여백 없이 실제 화면 그대로인지, (c) 파일이 `.jpg`이고 용량이 줄었는지, (d) 로그탭 사진 목록에 정상적으로 뜨고 썸네일/다운로드/전송이 동작하는지 아직 확인 안 됨.
2. [이월, 37차] 락 수정의 실제 동시성 재현 검증(의도적으로 거의 동시에 두 업로드 시도).
3. [이월, 34차] 도로명-신호과속 같은 줄 배치 확인(신호과속 배지가 나타나는 구간에서).
4. [이월] 28~30차 레이아웃 정밀 재검증.
5. [이월] "선택 다운로드" 버튼 실제 동작(다운로드 성공 여부)은 여전히 미확인 -- 이번 세션은 "선택 전송"만 확인됨.
6. [이월] 46차까지 확인된 항목을 제외한 나머지 코드 변경 전체 실차 재검증(12절 원칙).
7. [이월] 실기기 터미널로 배포된 tools.js에 "web-gdrive-connect" 문자열 실제 존재 여부 확인(핵심 발견 16).
8. [이월] test_web_upload.py 실제 실행해 낡은 테스트 수 확인 -> 데드코드 3개(tmux_web_target/resolve_upload_target/upload_target_settings) + 대응 테스트 삭제/갱신.
9. [이월] docs/carrot_web_upload.md 갱신(Drive 기준).
10. [이월] carrot-ms 모델 셀렉터 코드 분석 착수. WIP_SYNC.md 40차 체크포인트 반영 확인 필요.
11. [사용자 승인 필요, 이월] 핵심 발견 31의 재발 방지 제안(스크립트 파일명 버전 표시 규칙화) 채택 여부 결정.
12. [낮은 우선순위, 이월] WIP.md 파일 안 "# WIP" 헤더 중복(45차-정정에서 생긴 것으로 추정) 근본 정리 미실시.

검증: 사용자 제공 실기기 스크린샷 7장(구글드라이브에 실제 업로드된 파일 존재까지 확인) + GitHub API/raw 조회(SHA 고정)로 47차 커밋 실반영 확인. 47차 수정 자체의 촬영 결과물(실제 스크린샷 버튼으로 찍은 이미지)은 이번 세션에서 제공되지 않아 미확인.

주의사항:
- 이번 세션은 devnotes만 갱신하며 코드 변경이 없다. carrot-ryu에는 아무 스크립트도 적용하지 않는다(아래 반영 스크립트는 carrot-ryu-note 전용).

다음 작업 후보:
1. 47차 스크린샷 버튼 실제 촬영 결과 확인(가로/JPG/용량/목록 표시, 최우선)
2. 37차 락 동시성 재현 검증(의도적 동시 업로드 2회 시도)
3. 34차 도로명-신호과속 같은 줄 배치 확인(신호과속 구간 실주행 필요)
4. "선택 다운로드" 버튼 실제 동작 확인
5. test_web_upload.py 실행 + 데드코드 3개 삭제 착수
