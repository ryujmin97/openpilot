# HANDOFF

Worker: Claude (46차 -- HEAD 99b49a1 실기기 검증 4건, 최우선 이월 항목 전부 해소)
Date: 2026-09-16
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (HEAD: `99b49a113e48ddaeebf83074b04e42d417a88612`, 코드 변경 없음 -- 45차 최종본 그대로)
Note Branch: carrot-ryu-note (이 커밋으로 devnotes 갱신. base: `313ae77d244cde2eb5e2f2f8781aba4965af6496`, 45차-정정)
carrot-ms 마지막 검토/동기화 커밋(메시지 기준): 7차 체크포인트 이후 신규 23건 확인, WIP_SYNC.md 40차 체크포인트 반영 스크립트 실행 여부 미확인(계속 이월)

작업:
사용자가 제공한 실기기 스크린샷 2장(도구 탭 git pull/reboot 로그, 로그 탭 사진목록 화면)과 실기기 촬영 영상 1개(20260916-095343.mp4, 온로드 화면 4초)로, 직전 HANDOFF의 "최우선" 이월 1~4번을 전부 실기기 검증 완료. 코드 변경 없음, devnotes만 갱신.

완료:
1. carrot-ryu HEAD(`99b49a1`) 실기기 배포 확인: 도구 탭 로그에서 실제 `git pull` 출력(`Updating e2f356198..99b49a113`, `Fast-forward`, 변경 파일 `asset-manifest.json`/`js/generated/logs.js`)과 이어지는 `reboot` 실행을 확인. 45차 최종본이 실제로 실기기에 반영/재부팅됨을 최초로 실증(45차-정정까지는 "미실시" 상태였음).
2. 사진 목록 크래시 해소 확인(44/45차 formatLogBytes): 재부팅 후 로그 탭에서 사진 목록이 정상 렌더되고 파일 크기가 "2.2 MB"/"2.3 MB"로 정확히 포맷됨.
3. delete_all_videos 스크린샷 포함 삭제 확인(44차): 사용자가 실기기에서 전체 삭제 시 사진까지 삭제됨을 확인(사용자 보고).
4. 로그탭 새로고침 아이콘 확인(41차): 에러 없이 동작하고, 실제로 목록이 갱신됨을 사용자가 확인(사용자 보고). 41차 이월 항목 완료.
5. 녹화 버튼 깜빡임 확인(44차 3번, 정량 분석): 업로드된 영상을 ffmpeg로 6fps(24프레임) 추출, 버튼 좌표(2160x1080 기준 x:1212-1288, y:912-988)를 색상 필터로 특정해 프레임별 평균 RGB/밝기를 측정. 밝은 상태(R≈193, 밝기≈99)와 어두운 상태(R≈33, 밝기≈34)가 프레임마다 규칙적으로 교대됨을 확인 -- 직전 세션(1.8초 영상, 정지 프레임 육안 비교)에서는 판단 불가였던 것을 영역 크롭+수치 비교로 실증(FINDINGS.md 2026-09-16(46차) 항목).

미완료 (다음 세션 이월):
1. [이월, 37차] 락 수정의 실제 동시성 재현 검증(의도적으로 거의 동시에 두 업로드 시도).
2. [이월, 34차] 도로명-신호과속 같은 줄 배치 확인(신호과속 배지가 나타나는 구간에서).
3. [이월] 28~30차 레이아웃 정밀 재검증.
4. [이월] 46차까지 확인된 항목을 제외한 나머지 코드 변경 전체 실차 재검증(12절 원칙).
5. [이월] 실기기 터미널로 배포된 tools.js에 "web-gdrive-connect" 문자열 실제 존재 여부 확인(핵심 발견 16).
6. [이월] test_web_upload.py 실제 실행해 낡은 테스트 수 확인 -> 데드코드 3개(tmux_web_target/resolve_upload_target/upload_target_settings) + 대응 테스트 삭제/갱신.
7. [이월] docs/carrot_web_upload.md 갱신(Drive 기준).
8. [이월] carrot-ms 모델 셀렉터 코드 분석 착수. WIP_SYNC.md 40차 체크포인트 반영 확인 필요.
9. [사용자 승인 필요, 이월] 핵심 발견 31의 재발 방지 제안(스크립트 파일명 버전 표시 규칙화) 채택 여부 결정.
10. [낮은 우선순위, 이월] WIP.md 파일 안 "# WIP" 헤더가 맨 위 외에 본문 중간(45차-정정 항목과 44차 항목 사이)에도 한 번 더 있는 중복 -- 45차-정정에서 맨 위 헤더를 복원하며 생긴 것으로 추정, 근본 정리는 미실시(46차 반영 시에는 `^`로 시작 위치만 앵커링해 안전하게 우회함).

검증: 실기기 스크린샷 2장 + 실기기 촬영 영상 1개(사용자 제공)를 근거로 위 5개 항목 확인. 코드 자체는 이번 세션에서 변경하지 않음.

주의사항:
- WIP.md에 새 항목을 추가하는 반영 스크립트를 만들 때, 파일 맨 위 "# WIP" 헤더가 중복 존재하므로(위 미완료 10번) 일반적인 "앵커 정확히 1회 매치" 방식 대신 문자열 시작(`^`)에만 매칭되는 정규식을 사용해 맨 위에만 삽입하도록 처리했다. 이후 세션에서도 이 파일을 다룰 때는 동일하게 주의할 것.

다음 작업 후보:
1. 34차 도로명-신호과속 같은 줄 배치 확인(신호과속 구간 실주행 필요)
2. 37차 락 동시성 재현 검증(의도적 동시 업로드 2회 시도)
3. WIP.md 헤더 중복 정리(낮은 우선순위)
4. test_web_upload.py 실행 + 데드코드 3개 삭제 착수
5. carrot-ms 신규 23건 중 모델 셀렉터 관련 3건 cherry-pick 검토 착수(WIP_SYNC.md 40차 체크포인트)
