Worker: Claude (159cha, Claude Sonnet 5)
Date: 2026-09-25
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (HEAD: 619998bb144c699f379a5c8f6c9a64843d108f28, 156차 이후 코드 변경 없음 -- 157/158/159차 모두 devnotes만)
Note Branch: carrot-ryu-note (이 스크립트 반영 전 base: 31698eb05730277d1976c0fde875581752f96e72, 158차 devnotes push 완료 상태)
carrot-ms 마지막 검토/동기화 체크포인트: `3756e6d5`(130차, 139차 재확인 -- 신규 커밋 없음. 140~159차는 재점검 없음)

작업:
1. 세션 시작 4절 0단계로 지침 문서(v2, `31698eb`) 조회, `git ls-remote`로 carrot-ryu-note HEAD `31698eb`, carrot-ryu HEAD `619998bb` 확인(괴리 없음).
2. 사용자가 158차 결과를 붙여넣고 후보 수정안 1번(V_CURVE_LOOKUP_BP 첫 구간 완화)이 근본 해결이 아닌지 질문. 정적 계산과 같은 rlog zip(`HYUNDAI_GENESIS_541384155f4f8ca5_20260924_185642.zip`, 38세그먼트, 로그 기록 커밋 `f23d05f`) 재생으로 158차 결론을 재검증.

완료:
1. 158차 서술 정정 2건: (a) 사례 곡률 0.0015~0.0023은 테이블 첫 구간이 아니라 2~4번째 구간, (b) 후보 수정안 2번(`abs(curvature)<0.02` 상향)은 코드상 효과 없음 -- 2번 추천 철회. FINDINGS.md 핵심 발견 58, WIP.md 159차에 기록.
2. 영향 범위 재측정(32세그먼트, 38,401사이클, OLD=NEW=`f23d05f`): route 후보 급변 223건 중 94%(210건)는 자차속도+20km/h 위 영역이라 최종 목표속도(`min(speed_n_sources)`)에 영향 없음. route-source 2,432사이클 중 desiredSpeed 20km/h 초과 급변은 19건(0.8%, 트리거 0건, 18건은 안내 지점 접근 구간).
3. 1번안(테이블 완화)은 잡음을 억제하려면 80~130km/h 대역 평탄화가 필요해 실제 커브 반응을 왜곡한다고 판단 -- 입력(곡률) 쪽 필터가 더 맞다(정적 계산 기반, 사용자 승인 전, 실주행 검증 아님).

미완료(다음 세션 우선순):
1. 최우선: 안내 지점 부근 desiredSpeed 급변 19건의 군집(seg8 t 43~53s, seg15 27~31s, seg31 12~20s, 필요시 seg9/25/29)을 사이클 단위로 분석 -- 곡률 배열, 재보간 경로 모양, 로그 naviPaths 대조로 곡률 잡음 vs 경로 기하 급변을 분리. 결과에 따라 route 후보 필터(158차 후보 3번)를 좁게 적용할지 제안(사용자 승인 후 구현). 재현 절차는 WIP.md 159차 "재생 재현" 참고(zip 재제공 필요, 추출 38개+재생 32개 약 3분, 인라인 집계 코드는 toolkit 미등록).
2. 156차 A안 실기기 검증(강제 종료/전원 차단 재현) -- 155~156차부터 이월, 로그 분석 불가.
3. 톨게이트(xTurn=6) 구간 실차 검증 -- 이번 로그도 xTurn=6 전이 0건(114차부터 이월).
4. (선택) zip 무결성/용량 확인, `build_zip()` 디스크 여유 확인 코드(155차 이월).
5. 148차 v1 `2>&1` 재발의 FINDINGS.md 정식 등록 여부, 147차 임시 스크립트 toolkit 미등록(이전 이월, 변동 없음).

검증:
- 159차 재검증은 정적 계산(테이블 기울기)과 실제 코드(재구현 없이 ast 추출/exec) 기반 rlog open-loop 재생이며 실기기 테스트/실주행이 아니다.
- "영향 없음" 판정은 `vEgo+20km/h`를 임시 기준으로 쓴 것(실제 크루즈 설정값은 로그에 없음), decel 0.8/base 1.2는 toolkit 기본 가정(base 1.35에서는 급변 268건, 트리거 1건은 동일).

주의사항:
- 159차도 코드 변경이 없다(carrot-ryu HEAD는 156차 그대로 `619998bb`).
- 158차 WIP/FINDINGS는 불변이므로 정정은 159차 회차와 핵심 발견 58에만 기록했다. 158차 "후보 수정안 2번 추천"은 철회된 상태.
- navRoute=0인 6개 세그먼트(4, 34~38)는 이 도구로 재생 불가(32/38 커버).

다음 작업:
1. 사용자가 devnotes 반영 스크립트(`159cha_devnotes_carrot_ryu_note-v1.ps1`) 실행 -> push 확인.
2. 위 미완료 1번(군집 사이클 분석)부터 진행, 같은 rlog zip을 다시 제공받는다.
3. 실기기에서 강제 종료/전원 차단 재현(156차 A안) 또는 톨게이트 통과 로그 확보 시 그쪽도 병행 가능.