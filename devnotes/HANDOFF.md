# HANDOFF

Worker: Claude (38차 -- 36차/37차/34차 실기기 검증 1차 진행, 코드 변경 없이 devnotes만 갱신)
Date: 2026-09-15
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (변경 없음, 37차 커밋 그대로 유지)
Note Branch: carrot-ryu-note (이 커밋으로 devnotes 갱신)
carrot-ms 마지막 검토/동기화 커밋(메시지 기준): 7차 세션과 동일, 신규 커밋 없음(이번 세션에서도 재확인하지 않음)

작업:
사용자가 실기기 스크린샷 9장(온로드 HUD 1장, 대시캠 탭 로그 전송 플로우 3장, 화면녹화 탭/도구 탭/햄버거 메뉴 3장, 구글드라이브 앱 2장)을 제공. 37차 HANDOFF 미완료 목록 중 1번(36차 변경사항 실기기 검증)과 4번(34차 UI 실기기 재확인)을 함께 검증. 코드 변경은 없고 devnotes(WIP/FINDINGS/HANDOFF/CURRENT_STATUS)만 갱신.

완료:
- 36차 "당근서버" 라벨 오표시 버그 수정 확인 -- 대시캠 탭 세그먼트 "로그 전송" 확인 다이얼로그가 "구글 드라이브"로 정상 표시되고, 실제 전송도 "전송 완료 1/1"(qcamera 1개+rlog 1개, 11.2MB)로 성공함을 확인.
- 36차 햄버거 메뉴 화면녹화 탭 분기 확인 -- 화면녹화 탭에서 우측상단 메뉴를 열면 "로그 메뉴"에 정렬 옵션만 있고 "최근 로그 업로드" 섹션이 없음을 확인(35차 증상 3 해소).
- 34차 도착 텍스트 축소(40->32) 확인 -- 경로안내 박스에서 회전아이콘 초록박스와 도착 텍스트가 겹치지 않음을 확인(34차 목표 1 달성).
- Drive 폴더 단일화 정황 확인 -- 내 드라이브에 "CarrotWeb Logs" 폴더가 1개만 존재, 이번 세션 업로드 3건이 모두 같은 폴더에 들어감을 확인(37차 수정과 정합적이나 동시성 직접 재현은 아님, 아래 미완료 2번 참고).
- raw.githubusercontent.com 브랜치-head 캐시 지연 재현(핵심 발견 21과 동일 패턴) -- 37차 push 직후 재조회 시 이전(36차) 내용이 보였고, commit-pinned raw URL로 우회해 실제 최신 내용을 확인함. 상세: FINDINGS.md 2026-09-15(38차) 항목.

미완료 (다음 세션 이월):
1. [신규] 화면녹화 탭 업로드 UI(체크박스/전체선택/다운로드/전송) 자체 동작 검증 -- 실제 화면녹화 파일이 없어("화면녹화 기록이 없습니다") 테스트 대상 부재. 화면녹화본 확보 후 재검증 필요.
2. [신규] 37차 락 수정의 실제 동시성(거의 동시 호출) 재현 검증 -- 이번 검증의 업로드 3건은 14:48/14:50/14:52로 수 분 간격이 있어 레이스 조건의 직접 재현은 아님. 짧은 시간 안에 대시캠 탭 전송과 화면녹화 탭 전송(또는 대시캠 탭 개별/그룹 전송)을 의도적으로 동시에 눌러보는 재현 테스트가 필요.
3. [신규] 34차 도로명-신호과속 같은 줄 배치 확인 -- 이번 스크린샷 촬영 시점에 신호과속 배지가 화면에 없어(과속/신호 구간 아님) 비교 불가. 도로명("대덕대로989번길")이 경로안내 박스 바깥쪽 하단(IP 주소와 같은 줄)에 표시되고 있어, WIP 34차 기록("박스 안쪽, 신호과속과 같은 줄")과 실제로 일치하는지 신호과속 구간에서 재검증 필요.
4. [이월] 28~30차 레이아웃 정밀 재검증(이번엔 육안 확인만 완료, 좌표 단위 정밀 확인 아님).
5. 실차 재검증(8~37차 코드 변경 전부, 12절 원칙) -- 이번 세션에 일부 진행됨(위 완료 항목들), 나머지는 계속 이월.
6. 실기기에서 직접 디버깅: 배포된 tools.js에 "web-gdrive-connect" 문자열 실제 존재 여부(26차부터 이월).
7. test_web_upload.py 실제 실행해 낡은 테스트 수 확인 -> 데드코드 3개 + 대응 테스트 삭제/갱신(25차부터 이월).
8. docs/carrot_web_upload.md 갱신(Drive 기준)(15차부터 이월).
9. run_upload_segments() 설계 변경 실사용 문제 없는지 재확인(16차부터 이월).
10. carrot-ms 모델 셀렉터 코드 분석 착수(6차 이후 계속 미착수).

검증: 사용자 제공 실기기 스크린샷 9장을 이미지별로 대조 확인. 코드 변경이 없어 py_compile 등 정적 검증은 해당 없음. 실기기 검증: 위 "완료" 항목은 실시함(부분/정황적 확인 포함), 나머지 미완료 항목은 미실시.

주의사항:
- raw.githubusercontent.com은 push 직후 브랜치-head URL이 한동안 이전 내용을 반환할 수 있음(33차, 38차 재현). 다음 세션은 push 직후 devnotes/코드 재확인 시 commit-pinned raw URL(`/{sha}/...`) 또는 commit diff 엔드포인트를 우선 사용할 것.
- 37차 asyncio.Lock은 프로세스 내부에서만 유효하다는 한계가 여전히 남아있음(carrot_man.py의 send_tmux_web()은 별도 프로세스).
- 다음 세션 시작 시 4절 3번 단계(carrot-ryu 최신 commit 확인)에서 이번 세션에 코드 변경이 없었음(37차 커밋 그대로)을 확인할 것.

다음 작업 후보:
1. 화면녹화 탭 업로드 UI 자체 동작 검증(녹화본 확보 후)
2. 37차 락 수정 동시성 재현 검증(의도적으로 동시에 두 업로드 시도)
3. 34차 도로명-신호과속 같은 줄 배치 확인(신호과속 구간에서)
4. test_web_upload.py 실행 + 데드코드 정리
5. carrot-ms 모델 셀렉터 코드 분석 착수