# HANDOFF

Worker: Claude (60차 -- devnotes 파일 오염(HANDOFF.md/WIP_SYNC.md) 발견 및 복구. 코드 변경 없음)
Date: 2026-09-16
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (base: `9ccf1206a034c5fb5e5f35201553f9fc4e5237e5`, 55cha flip fix. 이번 세션 코드 변경 없음)
Note Branch: carrot-ryu-note (base: `77d5eed6af4131207e573f4e31791ee54115091b`, 59차 시점 devnotes. 이 커밋으로 devnotes 갱신)
carrot-ms 마지막 검토/동기화 커밋(메시지 기준): 904fd107b529f4636846bedcf645e102fce007b7, 59차와 동일(변경 없음).

작업:
세션 시작 시 4절 0단계에 따라 SHA(77d5eed) 고정으로 devnotes 파일을 직접 재조회하던 중, `devnotes/HANDOFF.md`(59차분)에 정상 내용 뒤로 PowerShell 히어스트링 조각(`'@` 및 `$WipSyncContent = @'`)과 함께 `devnotes/WIP_SYNC.md`의 전체 내용이 그대로 이어붙어 있는 것을 발견. 그 결과 실제 `devnotes/WIP_SYNC.md`는 0바이트(빈 파일)로 커밋되어 있었음. 59차(또는 그 이전) 반영 스크립트가 여러 교체형 파일을 한 스크립트에 담는 과정에서 히어스트링 종료 처리(`'@`)에 문제가 있었던 것으로 추정. 16절 원칙에 따라 임의로 넘어가지 않고 raw 조회(SHA 고정) 결과를 바이트 단위(Python)로 직접 대조해 오염을 확인한 뒤, HANDOFF.md 안에 남아있던 WIP_SYNC 원문을 그대로 추출해 복구했다. 복구된 WIP_SYNC.md 내용은 57~59차 체크포인트를 포함해 끝까지(6차 이전 이력 문단) 온전했으며, 누락된 부분은 없었다(끝부분 히어스트링 종료 따옴표 `'@`만 없이 파일이 그대로 끝나 있었음 -- 내용 자체의 손실은 없음).

완료:
1. devnotes/HANDOFF.md 오염 확인(SHA 77d5eed 고정 조회 + Python 바이트 비교로 직접 재현/검증).
2. devnotes/WIP_SYNC.md가 실제로 0바이트임을 raw 조회로 직접 확인.
3. HANDOFF.md 안에 섞여 있던 WIP_SYNC.md 원문을 정확한 경계(히어스트링 오프너/클로저 위치)로 분리 추출, 내용 무결성 확인(57~59차 체크포인트 전부 포함, 손실 없음).
4. HANDOFF.md를 정상 60차 내용으로 교체하는 반영 스크립트 작성(실행 대기).
5. WIP_SYNC.md를 복구 내용 + 60차 체크포인트(이번 오염/복구 기록)로 복원하는 반영 스크립트 작성(실행 대기).
6. WIP.md에 60차 항목 추가 스크립트 작성(실행 대기).
7. CURRENT_STATUS.md에 60차 항목 추가 스크립트 작성(실행 대기).

미완료 (다음 세션 최우선):
0. [신규, 최우선] 이번 복구 스크립트 실행 확인(push 로그 + raw 재조회로 HANDOFF.md/WIP_SYNC.md 정상 여부 직접 재검증, 16절).
0-1. [신규] 오염이 정확히 몇 차 세션의 반영 스크립트에서 발생했는지는 불명(59차로 추정되나 확정 아님) -- 필요하면 `git log -p -- devnotes/HANDOFF.md`로 커밋 이력 추적 가능. 우선순위는 낮음(내용은 이미 전부 복구됨).
1. [59차에서 이월, 최우선] 20절 절차의 실제 첫 리셋(carrot-ms 최신 HEAD 기준 carrot-ryu 재생성 + carrot-ryu-v1의 코드 수정 현황 36개 항목 이식) 착수 여부/순서를 사용자와 다시 확인. 이번 세션에서 사용자에게 재승인 여부를 질의했으나 이 HANDOFF 작성 시점까지 별도 확답 없음 -- 다음 세션 시작 시 최우선으로 재확인.
1-1. [59차에서 이월] 아래 2~7번(기존 2절 개별 선별 반영 이월분, 클러스터 A/B/C 등)을 20절 리셋으로 흡수할지, 그대로 2절 절차로 마저 진행할지 사용자와 확정 필요.
2. [최우선, 이월] 종방향 gap/lead-response 대개편 클러스터(154f819e, 48b57971, 6c1a4499, 1d956ec7, 0201cd19, 33427531, 3cff6956, 64921a81, 25af28f1, 904fd107 -- 10건) 정밀 diff 대조.
3. [이월] Hyundai CAN 상태 클러스터(0b0c282c, 66e75d87 -- 2건) 정밀 대조.
4. [이월] Cinque v2 eGPU 클러스터(b483035d, df0027f7, 544dffe -- 3건) 정밀 대조.
5. [이월] 저위험 3건 개별 확인 후 반영 여부 결정: 685ec5ce, 59617d38, 6b370dc7.
6. [이월] 빌드 인프라 2건(e9b3b89b, c5f6e95b) 별도 판단.
7. [이월] 6f63ad35(외부내비 우선) 실제 반영은 사용자 의도 확인 후 진행.
8. [이월] CURRENT_STATUS.md 항목 36을 "반영 완료(commit 9ccf1206)"로 정정.
9. [이월] 핵심 발견 31/37을 9절 정식 규칙으로 채택할지 확인(19절).
10. [이월, 37차] 락 수정 동시성 재현 검증.
11. [이월, 34차] 도로명-신호과속 같은 줄 배치 확인.
12. [이월] 28~30차 레이아웃 정밀 재검증.
13. [이월] "선택 다운로드" 버튼 실제 동작 미확인.
14. [이월] test_web_upload.py 데드코드 3개 삭제/갱신.
15. [이월] docs/carrot_web_upload.md 갱신.

검증: 이번 세션 코드 변경 전혀 없음, 실차 검증 대상 없음(12절). devnotes 오염 확인/복구는 raw.githubusercontent.com(SHA 고정) 조회 결과를 Python으로 바이트 단위 직접 비교해 수행 -- 텍스트 눈으로만 확인한 것이 아님.

주의사항:
- 이번 오염의 근본 원인으로 추정되는 패턴: 한 스크립트 안에서 여러 교체형 devnotes 파일(HANDOFF.md, WIP_SYNC.md 등)을 각각 다른 히어스트링 변수에 담을 때, 앞 변수의 종료 따옴표(`'@`)가 누락되거나 위치가 어긋나면 뒤 변수의 내용이 앞 파일에 그대로 흘러들어갈 수 있다. 이번 60차 반영 스크립트는 파일별로 종료 따옴표 위치를 재확인했으며, 향후 세션에서도 교체형 파일을 여러 개 한 스크립트에 담을 때는 각 히어스트링의 여는 줄(`$Var = @'`)과 닫는 줄(`'@`, 반드시 줄 시작)이 정확히 1:1로 쌍을 이루는지 재확인 필요.
- 20절 재생성(carrot-ryu를 carrot-ms 베이스로 재구성)은 여전히 미실행 상태. v1 아카이브만 생성되어 있고 carrot-ryu 자체는 9ccf1206 그대로.

다음 작업 후보:
1. 복구 스크립트 실행 확인(최우선)
2. 20절 리셋 착수 여부 재확인 후 진행
3. 종방향 클러스터(A) 정밀 분석 착수
4. Hyundai CAN 클러스터(B) 정밀 분석
5. eGPU 클러스터(C) 정밀 분석