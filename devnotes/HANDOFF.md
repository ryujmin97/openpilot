Worker: Claude (160cha, Claude Sonnet 5)
Date: 2026-09-25
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (HEAD: 619998bb144c699f379a5c8f6c9a64843d108f28, 156차 이후 코드 변경 없음 -- 157/158/159/160차 모두 devnotes만)
Note Branch: carrot-ryu-note (이 스크립트 반영 전 base: 5e255e29cc091691e1e2e84f1dd866228c60b3ed, 159차 devnotes push 완료 상태)
carrot-ms 마지막 검토/동기화 체크포인트: `3756e6d5`(130차, 139차 재확인 -- 신규 커밋 없음. 140~160차는 재점검 없음)

작업:
1. 세션 시작 4절 0단계로 지침 문서(v2, `5e255e2`) 조회, `git ls-remote`로 carrot-ryu-note HEAD `5e255e2`, carrot-ryu HEAD `619998bb` 확인(괴리 없음).
2. 159차 미완료 1번(안내 지점 부근 desiredSpeed 급변 19건 군집: seg8 t43~53s/seg15 27~31s/seg31 12~20s, seg9/25/29 포함)을 사용자가 다시 제공한 같은 rlog zip(`HYUNDAI_GENESIS_541384155f4f8ca5_20260924_185642.zip`, 38세그먼트, 기록 커밋 `f23d05f`)으로 사이클 단위 분석.

완료:
1. 19건 전부 분류: A. 정상 소스 전환(10건, desiredSource가 다른 후보로 넘어간 정상 동작), B. 정상 폴리라인 교체(1건, seg8 t=43.63s, navi_points_start_index 리셋), C. 곡률 잡음(8건, 158/159차 메커니즘). 상세는 WIP.md 160차 참고.
2. 로그 `route=` 필드와 재생값(r_o) 사이에 1사이클(20Hz, 0.03~0.06s) 지연이 있음을 발견 -- 같은 사이클 대조로는 C 중 2건만 일치했으나 -1 시프트 적용 후 9건 중 8건 재현. decel 0.3~2.0 스윕으로 타이밍 패턴이 재생 파라미터에 의존하지 않음을 확인(원인 자체는 미확정, 정적 추적 추정만). toolkit README에 방법론 노트 추가.
3. `navRoute` 메시지가 `limit_route_points(4096)`로 잘렸을 가능성 가설 기각(실측 포인트 수 43~239개, 4096에 한참 못 미침).
4. C(8건)가 몰린 3개 구간(seg8 t 52.6~53.3s 부근 xTurn=1, seg15 t 27.4~28.5s 부근 xTurn=3, seg31 t 14.4s 부근 xTurn=4)에 158차 후보 3번(route 후보 사이클간 슬루레이트/저역통과)을 곡률 크기(|curvature|<0.003)로 범위를 좁혀 적용하는 설계를 제안(코드 미반영, 파라미터 미정, 사용자 승인 대기).

미완료(다음 세션 우선순):
1. 최우선: 위 후보 3번 좁은 설계의 구체 파라미터(사이클간 변화 상한, 곡률 상한) 확정 -- 사용자 승인 후 10절 최소변경으로 구현.
2. 1사이클 지연의 근본 원인 확정(선택, 급하지 않음 -- 필터 설계 자체는 지연 유무와 무관하게 유효).
3. 156차 A안 실기기 검증(강제 종료/전원 차단 재현) -- 155~156차부터 이월, 로그 분석 불가.
4. 톨게이트(xTurn=6) 구간 실차 검증 -- 이번 로그도 xTurn=6 전이 0건(114차부터 이월).
5. (선택) zip 무결성/용량 확인, `build_zip()` 디스크 여유 확인 코드(155차 이월).
6. 148차 v1 `2>&1` 재발의 FINDINGS.md 정식 등록 여부, 147차 임시 스크립트 toolkit 미등록(이전 이월, 변동 없음).

검증:
- 160차 분석은 open-loop 로그 재생(재구현 없이 ast 추출/exec 기반)이며 실기기 테스트/실주행이 아니다.
- C(8건) 판정은 -1 사이클 시프트 가정(정적 추적 기반 추정) 적용 후 결과이며, 이 지연의 근본 원인 자체는 확정하지 않았다.

주의사항:
- 160차도 코드 변경이 없다(carrot-ryu HEAD는 156차 그대로 `619998bb`).
- 후보 3번 구현은 사용자 승인 전까지 보류 상태(10절/15절/18절: 코드 변경은 항상 사용자 승인 후에만 진행).

다음 작업:
1. 사용자가 devnotes 반영 스크립트(`160cha_devnotes_carrot_ryu_note-v1.ps1`) 실행 -> push 확인.
2. 후보 3번 설계에 대한 사용자 승인/파라미터 결정 -> 승인되면 다음 세션에서 코드 구현.
3. 실기기에서 강제 종료/전원 차단 재현(156차 A안) 또는 톨게이트 통과 로그 확보 시 그쪽도 병행 가능.
