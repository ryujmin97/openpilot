Worker: Claude (158cha, Claude Sonnet 5)
Date: 2026-09-25
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (HEAD: 619998bb144c699f379a5c8f6c9a64843d108f28, 156차 이후 코드 변경 없음 -- 157/158차 모두 devnotes만)
Note Branch: carrot-ryu-note (이 스크립트 반영 전 base: 9e2e4087eb1e56de8a16087bc12e7be3f5b49e18, 157차 devnotes push 완료 상태)
carrot-ms 마지막 검토/동기화 체크포인트: `3756e6d5`(130차, 139차 재확인 -- 신규 커밋 없음. 140~158차는 재점검 없음)

작업:
1. 세션 시작 4절 0단계로 지침 문서(v2, `9e2e408`) 조회, `git ls-remote`로 carrot-ryu-note HEAD가 `9e2e408`임을 확인(0단계 기준과 일치, 괴리 없음).
2. 사용자가 157차와 동일 route(`00000449--b34152b780`, 38세그먼트, 기록 커밋 `f23d05f`)의 rlog zip을 재제공. 154차부터 이월되던 "잔여 비트리거 route 급변(세그먼트당 10~25건, 원인 미확정)" 근본 원인 분석에 착수.

완료:
1. 기존 toolkit(`route_decel/replay_route_geom.py`) 재사용, navRoute 있는 32/38세그먼트를 old=new=`f23d05f`(현재 코드, 153차 버그와 무관하게 고정) 동일 커밋으로 개별 재생. 38,401사이클 중 route 급변 223건 중 222건(99.6%)이 비트리거(153차 수정 범위 밖) 사이클에서 발생함을 확인.
2. 급변 사이클 쌍 다수(seg3/8/22/32)를 직접 대조해 근본 원인 확정: `carrot_man.py`의 `V_CURVE_LOOKUP_BP` 첫 구간(곡률 0→1/800, 속도 300→150km/h)이 테이블 전체에서 가장 가파른데, 매 사이클 새로 계산되는 곡률(차량 전진에 따른 재보간 잡음, 0.0005~0.001 단위 변동)이 이 구간에 자주 걸리며 20~80km/h대 속도 스윙으로 증폭됨을 확인. `out_speed_of()`의 후진 감속제한 루프도 관찰 사례 전부에서 제약이 안 걸려 잡음을 거르지 못함. FINDINGS.md 핵심 발견 57로 등록.
3. 후보 수정안 4가지 도출(WIP.md 158차 참고, 코드 미반영) -- 채택 여부는 사용자 승인 필요.

미완료(다음 세션 우선순):
1. 신규(최우선): 158차 후보 수정안 중 채택안 확인 -- 승인되면 10절 최소변경 원칙으로 구현.
2. 156차 A안 실기기 검증(강제 종료/전원 차단 재현) -- 155~156차부터 이월, 로그 분석 불가.
3. 톨게이트(xTurn=6) 구간 실차 검증 -- 이번 로그도 xTurn=6 전이 0건, 여전히 미확인(114차부터 이월).
4. (선택) zip 내용 무결성 확인, 두 번째 zip이 더 컸던 이유 확인(155차 이월).
5. (선택) `build_zip()` 직전 디스크 여유공간 사전 확인 코드.
6. 148차 v1 `2>&1` 재발의 FINDINGS.md 정식 등록 여부, 147차 임시 스크립트 toolkit 미등록(이전 이월, 변동 없음).

검증:
- 158차 분석은 실제 코드(재구현 없이 ast 추출/exec)로 실주행 rlog를 재생·집계한 open-loop 로그 분석이며, 실기기 테스트/실주행 자체는 아니다.
- 원인 분석은 "동일 커밋(f23d05f) 단독 재생"으로 153차 수정 유무 비교가 아니라 "현재 코드에 남은 잡음"만 분리해서 본 것이라, 157차의 OLD/NEW 비교 수치와는 산출 방식이 다르다(둘 다 유효하나 직접 비교 금지).

주의사항:
- 158차는 코드 변경이 없다(carrot-ryu HEAD는 156차 그대로 `619998bb`).
- 근본 원인은 확정했으나 수정 코드는 아직 작성/반영하지 않았다 -- 다음 세션 최우선 항목은 "분석"이 아니라 "수정안 승인 후 구현"이다.
- navRoute=0인 6개 세그먼트(4, 34~38)는 이 도구로 재생 불가해 이번 분석에서 제외됐다(전체 38개 중 32개=84% 커버리지).

다음 작업:
1. 사용자가 아래 devnotes 반영 스크립트(`158cha_devnotes_carrot_ryu_note.ps1`)를 실행 -> push 확인.
2. 158차 후보 수정안 검토 후 채택안 결정.
3. 실기기에서 강제 종료/전원 차단 재현(156차 A안) 또는 톨게이트 통과 로그 확보 시 그쪽도 병행 가능.