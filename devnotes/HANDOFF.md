# HANDOFF

Worker: Claude (세션 29)
Date: 2026-09-15
Repository: ryujmin97/openpilot
Code Branch: carrot-ryu (직전 HEAD: cc73f629, 28차 반영 / 이번 세션 코드 변경은 스크립트 준비만 완료, 사용자 미실행 상태)
Note Branch: carrot-ryu-note (이 커밋으로 29차 devnotes 반영)
carrot-ms 마지막 검토/동기화 커밋(메시지 기준): 7차 세션과 동일, 신규 커밋 없음 (29차에서도 재확인하지 않음)

작업:
사용자가 실기기 사진 한 장을 제공하며 우측하단 경로안내 박스(27~28차에서 조정한 그 박스)의 레이아웃을
추가로 세 가지 조정 요청함 — (1) "교차로"(제목) 텍스트를 위로 이동해 회전 아이콘 초록박스와 안 겹치게
(2) "도착:"/ETA 시간 텍스트를 pad(24px) 인셋이 아니라 박스 우측 경계에 끝맞춤 (3) "신호과속" 배지를
박스 맨 아래(다른 하단 상태줄과 겹쳐 보이던 위치)에서 바로 위 회전 아이콘 초록박스 하단에 붙는 위치로 이동.

이번 세션 시작 시 4절 절차를 끝까지 지키지 못함 — 0번(지침 문서), 1번(HANDOFF.md)만 확인하고 2번
(CURRENT_STATUS.md), 3번(carrot-ryu 최신 commit API 확인)을 건너뛴 채 바로 코드 스크립트를 만들었다가
사용자 지적으로 중간에 절차를 다시 밟음. 이 과정에서 CURRENT_STATUS.md가 27차(5f5e49d0) 기준으로
멈춰 있고 실제 HEAD는 28차(cc73f629)까지 진행돼 있어 이미 한 세션치 밀려 있었음을 추가로 확인함
(28차 HANDOFF.md에 "이번 세션은 WIP.md 갱신 안 함"이라는 기록은 있었으나 CURRENT_STATUS.md 미갱신
언급은 없었음 — 이번 세션에서 새로 발견).

완료:
- openpilot/selfdrive/ui/onroad/hud_renderer.py `_draw_turn_info_hud()` 수정안을 로컬에서
  문자열 블록 치환 4곳(제목 y좌표, ETA/거리 x좌표, bx/by를 if 밖으로, 신호과속 배지 y좌표)으로
  구성하고 각각 원본 파일에서 정확히 1회 매치되는 것을 확인, py_compile 통과 확인함.
- 위 내용을 반영하는 PowerShell 스크립트(carrot-ryu용, core.autocrlf=false 포함)를 파일로
  생성해 사용자에게 전달함.

미완료 (다음 세션 최우선):
1. **사용자가 코드 반영 스크립트를 아직 실행하지 않음** — 실행 전까지 이 레이아웃 변경은
   GitHub에 반영된 것이 아님(5절 원칙). 실행 로그(특히 git commit/push 출력)를 받으면
   carrot-ryu를 다시 clone해 diff 0 확인 + py_compile 재검증 필요(20차 원칙).
2. 오늘 변경분(교차로 위치, ETA 끝맞춤, 신호과속 위치)은 사진 한 장만으로 추정한 좌표이며
   실차 화면으로 재확인되지 않음(12절 원칙) — 반영 후 실기기 스크린샷으로 재검증 필요.
   특히 "신호과속" 배지는 199m 텍스트와의 세로 간격이 빠듯할 수 있어 겹침 여부 확인 필요.
3. CURRENT_STATUS.md가 27차(5f5e49d0) 기준에 멈춰 있음 — 28차(및 이번 29차 반영 후) 기준으로
   갱신 필요(사용자 승인 후 별도 스크립트로 처리 예정, 임의로 손대지 않음).
4. 실차 재검증(8~29차 코드 변경 전부 이월, 12절 원칙)
5. 실기기에서 직접 디버깅: 배포된 tools.js에 "web-gdrive-connect" 문자열 실제 존재 여부,
   브라우저 강제새로고침/시크릿모드 재현 여부 (26차부터 이월)
6. 화면녹화 전송 다이얼로그 "당근서버" 라벨 하드코딩 여부 코드 조사 (26차 발견, 미착수)
7. test_web_upload.py 실제 실행(또는 최소 import/픽스처 점검)해 낡은 테스트 수 확인 ->
   데드코드 3개(tmux_web_target, resolve_upload_target, upload_target_settings) + 대응 테스트
   삭제/갱신 (25차부터 이월)
8. 실제 Google Cloud Console OAuth 클라이언트 발급 + 콤마 기기 Drive 연결 테스트(device flow
   전체) (15차부터 이월)
9. docs/carrot_web_upload.md 갱신 (Drive 기준) (15차부터 이월)
10. run_upload_segments() 설계 변경 두 가지 실사용 문제 없는지 재확인 (16차부터 이월)
11. carrot-ms 모델 셀렉터 코드 분석 착수 (6차 이후 계속 미착수)
12. WIP.md에 29차(및 28차 누락분) 회차 요약 추가 필요 여부 검토

검증: 코드 파일 수정안은 로컬 파이썬 스크립트로 old 블록 1회 매치 + py_compile만 확인했고,
사용자 장치에서의 실제 git apply/push는 아직 이루어지지 않음. 실차 검증: 미실시(12절 원칙).

주의사항:
- 이번 세션에서 4절 절차 누락(2, 3번 단계 건너뜀)이 있었음을 사용자가 직접 지적함 — 다음
  세션은 0~3번 단계를 순서대로 전부 확인한 뒤 작업을 시작할 것.
- "신호과속" 배지 위치는 초록박스 "내부"가 아니라 초록박스 바로 "아래(붙임)"로 구현함 —
  199m 텍스트와 공간이 겹쳐 문자 그대로 박스 내부에 넣기 어려웠던 공학적 타협이며, 이 가정이
  틀렸다면(예: 실제로는 내부 배치를 원했다면) 다음 세션에서 재조정 필요.

다음 작업 후보:
1. 사용자 실행 로그 확인 -> GitHub 재조회로 diff 0 검증
2. 실기기 스크린샷으로 교차로/ETA/신호과속 위치 재확인, 필요 시 좌표 미세조정
3. CURRENT_STATUS.md 갱신(사용자 승인 후)
4. 실기기 터미널로 배포된 tools.js 내용 확인 (26차부터 이월, 원인 규명의 핵심)
5. carrot-ms 모델 셀렉터 코드 분석 착수